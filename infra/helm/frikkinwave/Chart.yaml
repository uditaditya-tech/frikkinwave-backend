apiVersion: v2
name: frikkinwave
description: |
  The frikkinwave backend on Kubernetes: Django/gunicorn web, the transactional-
  outbox relay, four Kafka consumer groups, and schema migrations as a
  pre-upgrade Job.
type: application

# Chart version — bump when the templates change shape.
version: 0.2.0

# The app version is supplied per-deploy as the image tag, so this is only a
# label. See infra/scripts/app-deploy.sh.
appVersion: "0.1.0"
